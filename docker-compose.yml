name : assist-chat-build
services: 
  db:
    image : postgres:16-alpine
    container_name: assist-chat-db
    environment: 
      POSTGRES_USER : postgres
      POSTGRES_PASSWORD : postgres
      POSTGRES_DB : postgres
    ports: 
      - "5430:5432"
    restart : always